<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat base required so that MediaRouteButton (used for Google Cast demo) renders
         correctly. The Compose MaterialTheme is applied inside setContent{} independently. -->
    <style name="Theme.BoomstreamExample" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
